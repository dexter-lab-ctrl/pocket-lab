# Security Architecture & Threat Model

!!! note "Generated Tier 6.5 threat-model page"
    This page is generated from repository metadata. Operation-level `security` blocks in `operations/*.yaml` are required for new features. OWASP Threat Dragon is a local review/editing tool, not the source of truth.

## Objective

Pocket Lab uses Threat Modeling as Code to document trust boundaries, data flows, attack surfaces, STRIDE threats, mitigations, residual risks, and evidence sources across the FastAPI + NATS / JetStream + Worker architecture.

## Source of Truth

| Field | Value |
|---|---|
| Model | pocketlab-threat-model |
| Tier | 6.5 |
| Generated at | 2026-06-15T15:47:37.885213+00:00 |
| Source rule | Repository-generated metadata. Operation-level security blocks in operations/*.yaml are required for new features. |

## Evidence Sources

| Type | Path / sample | Count | Notes |
|---|---|---|---|
| structurizr | architecture/structurizr/workspace.dsl |  | C4 architecture model and Tier 5B security views. |
| openapi | contracts/generated/openapi.json | 79 |  |
| asyncapi | contracts/asyncapi/pocketlab-nats-jetstream.yaml | 56 |  |
| typed_operations | contracts/operations/pocketlab-typed-operations.json | 19 |  |
| operation_security_metadata | operations/backup_now.yaml, operations/backup_verify.yaml, operations/catalog_refresh.yaml, operations/configure_opa.yaml, operations/deploy_blueprint.yaml, operations/drift_apply.yaml ... (19 total) |  |  |

## Tier 5B Architecture Views Used

- `security-review`
- `nats-command-event-boundaries`
- `audit-and-dlq-paths`

## Operation Security Metadata Coverage

| Operation | Classification | STRIDE | Trust Boundaries | Attack Surfaces |
|---|---|---|---|---|
| backup_now | recovery | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| backup_verify | recovery | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| catalog_refresh | operational | Tampering, Repudiation | browser_to_fastapi, fastapi_to_nats, nats_to_worker | api_endpoint, nats_subject, worker_handler |
| configure_opa | policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | api_endpoint, nats_subject, worker_handler, vault_or_policy_adapter, audit_event_payload |
| deploy_blueprint | operational | Tampering, Repudiation | browser_to_fastapi, fastapi_to_nats, nats_to_worker | api_endpoint, nats_subject, worker_handler |
| drift_apply | configuration | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| drift_approve | configuration | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| drift_ignore | configuration | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| drift_preview | configuration | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| drift_scan | configuration | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| fleet_join | secret | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | api_endpoint, nats_subject, worker_handler, vault_or_policy_adapter, audit_event_payload |
| git_sync | operational | Tampering, Repudiation | browser_to_fastapi, fastapi_to_nats, nats_to_worker | api_endpoint, nats_subject, worker_handler |
| health_check | telemetry | Tampering, Repudiation, Information Disclosure, Denial of Service | browser_to_fastapi, fastapi_to_nats, nats_to_worker | api_endpoint, nats_subject, worker_handler, telemetry_payload, status_stream |
| release_apply | release | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| release_check | release | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| restore_backup | recovery | Tampering, Repudiation, Denial of Service, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | api_endpoint, nats_subject, worker_handler, state_store, event_journal, dlq_subject |
| rotate_secret | secret | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | api_endpoint, nats_subject, worker_handler, vault_or_policy_adapter, audit_event_payload |
| secret_read_dynamic | secret | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | api_endpoint, nats_subject, worker_handler, vault_or_policy_adapter, audit_event_payload |
| security_scan | policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | api_endpoint, nats_subject, worker_handler, vault_or_policy_adapter, audit_event_payload |

## Protected Assets

| Asset | Classification | Evidence |
|---|---|---|
| FastAPI Control API | control-plane API | OpenAPI paths |
| NATS / JetStream | command and event backbone | AsyncAPI channels |
| Pocket Lab Worker | typed operation executor | Structurizr worker container |
| Typed Operations Catalog | execution contract | operations/*.yaml |
| Event Journal / Workflow Store | workflow recovery evidence | Tier 5B audit view |
| Audit Trail / Evidence Store | audit evidence | Tier 5B audit view |
| Vault / OpenBao Runtime | secret backend | Tier 5B security view |
| OPA Runtime | policy decision point | Tier 5B security view |

## Trust Boundaries

| Boundary | Description | Controls |
|---|---|---|
| browser_to_fastapi | Operation metadata declares crossing of browser_to_fastapi. | asyncapi_subject_governance, correlation_ids, dlq_review_path, event_redaction, event_sourced_workflow_journal, opa_policy_decision, openapi_validation, operation_audit_events, payload_minimization, secret_redaction |
| fastapi_to_nats | Operation metadata declares crossing of fastapi_to_nats. | asyncapi_subject_governance, correlation_ids, dlq_review_path, event_redaction, event_sourced_workflow_journal, opa_policy_decision, openapi_validation, operation_audit_events, payload_minimization, secret_redaction |
| nats_to_worker | Operation metadata declares crossing of nats_to_worker. | asyncapi_subject_governance, correlation_ids, dlq_review_path, event_redaction, event_sourced_workflow_journal, opa_policy_decision, openapi_validation, operation_audit_events, payload_minimization, secret_redaction |
| worker_to_secrets_policy | Operation metadata declares crossing of worker_to_secrets_policy. | asyncapi_subject_governance, correlation_ids, dlq_review_path, event_redaction, event_sourced_workflow_journal, opa_policy_decision, openapi_validation, operation_audit_events, payload_minimization, secret_redaction |
| worker_to_state_audit | Operation metadata declares crossing of worker_to_state_audit. | asyncapi_subject_governance, correlation_ids, dlq_review_path, event_redaction, event_sourced_workflow_journal, opa_policy_decision, openapi_validation, operation_audit_events, payload_minimization, secret_redaction |

## Data Flows

| Flow | Source | Destination | Trust Boundary | STRIDE |
|---|---|---|---|---|
| backup_now operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| backup_verify operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| catalog_refresh operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker | Tampering, Repudiation |
| configure_opa operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege |
| deploy_blueprint operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker | Tampering, Repudiation |
| drift_apply operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| drift_approve operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| drift_ignore operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| drift_preview operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| drift_scan operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| fleet_join operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege |
| git_sync operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker | Tampering, Repudiation |
| health_check operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker | Tampering, Repudiation, Information Disclosure, Denial of Service |
| release_apply operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| release_check operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| restore_backup operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit | Tampering, Repudiation, Denial of Service, Elevation of Privilege |
| rotate_secret operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege |
| secret_read_dynamic operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege |
| security_scan operation flow | React / Vite PWA | FastAPI → NATS / JetStream → Worker | browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy | Tampering, Repudiation, Information Disclosure, Elevation of Privilege |

## Attack Surfaces

| Surface | Evidence Count | Example Operations |
|---|---|---|
| api_endpoint | 19 | backup_now, backup_verify, catalog_refresh, configure_opa, deploy_blueprint, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, fleet_join, git_sync |
| audit_event_payload | 5 | configure_opa, fleet_join, rotate_secret, secret_read_dynamic, security_scan |
| dlq_subject | 10 | backup_now, backup_verify, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, release_apply, release_check, restore_backup |
| event_journal | 10 | backup_now, backup_verify, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, release_apply, release_check, restore_backup |
| nats_subject | 19 | backup_now, backup_verify, catalog_refresh, configure_opa, deploy_blueprint, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, fleet_join, git_sync |
| state_store | 10 | backup_now, backup_verify, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, release_apply, release_check, restore_backup |
| status_stream | 1 | health_check |
| telemetry_payload | 1 | health_check |
| vault_or_policy_adapter | 5 | configure_opa, fleet_join, rotate_secret, secret_read_dynamic, security_scan |
| worker_handler | 19 | backup_now, backup_verify, catalog_refresh, configure_opa, deploy_blueprint, drift_apply, drift_approve, drift_ignore, drift_preview, drift_scan, fleet_join, git_sync |

## STRIDE Threats

| ID | Source | Operation | Category | Scenario | Mitigations | Residual Risk |
|---|---|---|---|---|---|---|
| TM-S-001 | baseline |  | Spoofing | An untrusted client attempts to impersonate the UI, worker, or fleet agent. | API auth/write gates, NATS subject role separation, fleet join validation | Misconfigured local development credentials can weaken role boundaries. |
| TM-T-001 | baseline |  | Tampering | A caller modifies operation payloads, catalog data, or NATS command bodies. | typed operation schema validation, AsyncAPI subject governance, operation metadata checks | A compromised operator account can still submit authorized destructive changes. |
| TM-R-001 | baseline |  | Repudiation | A sensitive release, vault, drift, or restore action cannot be traced after execution. | correlation IDs, audit events, event-sourced workflow journal | Local storage loss can remove evidence unless backed up. |
| TM-I-001 | baseline |  | Information Disclosure | Secrets leak through logs, events, audit payloads, UI streams, or DLQ records. | redaction rules, journal redaction tests, DLQ payload review | New secret field names need continuous review. |
| TM-D-001 | baseline |  | Denial of Service | NATS, worker, OPA, Vault, or the API becomes unavailable during an operation. | readiness gates, JetStream retry/DLQ, degraded-mode tests | Single-device edge deployments remain capacity constrained. |
| TM-E-001 | baseline |  | Elevation of Privilege | A lower-privileged path triggers privileged operations such as release apply, restore, or secret rotation. | write authorization, OPA policy decisions, operation safety metadata | Policy gaps can appear when new operations are added without matching guardrails. |
| OP-BACKUP_NOW-TAMPERING | operation_metadata | backup_now | Tampering | backup_now may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_NOW-REPUDIATION | operation_metadata | backup_now | Repudiation | backup_now may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_NOW-DENIAL | operation_metadata | backup_now | Denial of Service | backup_now may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_NOW-ELEVATION | operation_metadata | backup_now | Elevation of Privilege | backup_now may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_VERIFY-TAMPERING | operation_metadata | backup_verify | Tampering | backup_verify may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_VERIFY-REPUDIATION | operation_metadata | backup_verify | Repudiation | backup_verify may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_VERIFY-DENIAL | operation_metadata | backup_verify | Denial of Service | backup_verify may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-BACKUP_VERIFY-ELEVATION | operation_metadata | backup_verify | Elevation of Privilege | backup_verify may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-CATALOG_REFRESH-TAMPERING | operation_metadata | catalog_refresh | Tampering | catalog_refresh may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-CATALOG_REFRESH-REPUDIATION | operation_metadata | catalog_refresh | Repudiation | catalog_refresh may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-CONFIGURE_OPA-TAMPERING | operation_metadata | configure_opa | Tampering | configure_opa may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-CONFIGURE_OPA-REPUDIATION | operation_metadata | configure_opa | Repudiation | configure_opa may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-CONFIGURE_OPA-INFORMATION | operation_metadata | configure_opa | Information Disclosure | configure_opa may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-CONFIGURE_OPA-ELEVATION | operation_metadata | configure_opa | Elevation of Privilege | configure_opa may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-DEPLOY_BLUEPRINT-TAMPERING | operation_metadata | deploy_blueprint | Tampering | deploy_blueprint may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-DEPLOY_BLUEPRINT-REPUDIATION | operation_metadata | deploy_blueprint | Repudiation | deploy_blueprint may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-DRIFT_APPLY-TAMPERING | operation_metadata | drift_apply | Tampering | drift_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPLY-REPUDIATION | operation_metadata | drift_apply | Repudiation | drift_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPLY-DENIAL | operation_metadata | drift_apply | Denial of Service | drift_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPLY-ELEVATION | operation_metadata | drift_apply | Elevation of Privilege | drift_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPROVE-TAMPERING | operation_metadata | drift_approve | Tampering | drift_approve may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPROVE-REPUDIATION | operation_metadata | drift_approve | Repudiation | drift_approve may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPROVE-DENIAL | operation_metadata | drift_approve | Denial of Service | drift_approve may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_APPROVE-ELEVATION | operation_metadata | drift_approve | Elevation of Privilege | drift_approve may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_IGNORE-TAMPERING | operation_metadata | drift_ignore | Tampering | drift_ignore may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_IGNORE-REPUDIATION | operation_metadata | drift_ignore | Repudiation | drift_ignore may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_IGNORE-DENIAL | operation_metadata | drift_ignore | Denial of Service | drift_ignore may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_IGNORE-ELEVATION | operation_metadata | drift_ignore | Elevation of Privilege | drift_ignore may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_PREVIEW-TAMPERING | operation_metadata | drift_preview | Tampering | drift_preview may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_PREVIEW-REPUDIATION | operation_metadata | drift_preview | Repudiation | drift_preview may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_PREVIEW-DENIAL | operation_metadata | drift_preview | Denial of Service | drift_preview may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_PREVIEW-ELEVATION | operation_metadata | drift_preview | Elevation of Privilege | drift_preview may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_SCAN-TAMPERING | operation_metadata | drift_scan | Tampering | drift_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_SCAN-REPUDIATION | operation_metadata | drift_scan | Repudiation | drift_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_SCAN-DENIAL | operation_metadata | drift_scan | Denial of Service | drift_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-DRIFT_SCAN-ELEVATION | operation_metadata | drift_scan | Elevation of Privilege | drift_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-FLEET_JOIN-TAMPERING | operation_metadata | fleet_join | Tampering | fleet_join may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-FLEET_JOIN-REPUDIATION | operation_metadata | fleet_join | Repudiation | fleet_join may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-FLEET_JOIN-INFORMATION | operation_metadata | fleet_join | Information Disclosure | fleet_join may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-FLEET_JOIN-ELEVATION | operation_metadata | fleet_join | Elevation of Privilege | fleet_join may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-GIT_SYNC-TAMPERING | operation_metadata | git_sync | Tampering | git_sync may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-GIT_SYNC-REPUDIATION | operation_metadata | git_sync | Repudiation | git_sync may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. |
| OP-HEALTH_CHECK-TAMPERING | operation_metadata | health_check | Tampering | health_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, payload_minimization, event_redaction | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Telemetry schemas can drift if new fields are added without privacy review. |
| OP-HEALTH_CHECK-REPUDIATION | operation_metadata | health_check | Repudiation | health_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, payload_minimization, event_redaction | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Telemetry schemas can drift if new fields are added without privacy review. |
| OP-HEALTH_CHECK-INFORMATION | operation_metadata | health_check | Information Disclosure | health_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, payload_minimization, event_redaction | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Telemetry schemas can drift if new fields are added without privacy review. |
| OP-HEALTH_CHECK-DENIAL | operation_metadata | health_check | Denial of Service | health_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, payload_minimization, event_redaction | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Telemetry schemas can drift if new fields are added without privacy review. |
| OP-RELEASE_APPLY-TAMPERING | operation_metadata | release_apply | Tampering | release_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_APPLY-REPUDIATION | operation_metadata | release_apply | Repudiation | release_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_APPLY-DENIAL | operation_metadata | release_apply | Denial of Service | release_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_APPLY-ELEVATION | operation_metadata | release_apply | Elevation of Privilege | release_apply may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_CHECK-TAMPERING | operation_metadata | release_check | Tampering | release_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_CHECK-REPUDIATION | operation_metadata | release_check | Repudiation | release_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_CHECK-DENIAL | operation_metadata | release_check | Denial of Service | release_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RELEASE_CHECK-ELEVATION | operation_metadata | release_check | Elevation of Privilege | release_check may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RESTORE_BACKUP-TAMPERING | operation_metadata | restore_backup | Tampering | restore_backup may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RESTORE_BACKUP-REPUDIATION | operation_metadata | restore_backup | Repudiation | restore_backup may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RESTORE_BACKUP-DENIAL | operation_metadata | restore_backup | Denial of Service | restore_backup may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-RESTORE_BACKUP-ELEVATION | operation_metadata | restore_backup | Elevation of Privilege | restore_backup may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_state_audit. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, event_sourced_workflow_journal, dlq_review_path, correlation_ids | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. Edge-first deployments remain sensitive to host, storage, and network availability. |
| OP-ROTATE_SECRET-TAMPERING | operation_metadata | rotate_secret | Tampering | rotate_secret may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-ROTATE_SECRET-REPUDIATION | operation_metadata | rotate_secret | Repudiation | rotate_secret may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-ROTATE_SECRET-INFORMATION | operation_metadata | rotate_secret | Information Disclosure | rotate_secret may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-ROTATE_SECRET-ELEVATION | operation_metadata | rotate_secret | Elevation of Privilege | rotate_secret may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECRET_READ_DYNAMIC-TAMPERING | operation_metadata | secret_read_dynamic | Tampering | secret_read_dynamic may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECRET_READ_DYNAMIC-REPUDIATION | operation_metadata | secret_read_dynamic | Repudiation | secret_read_dynamic may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECRET_READ_DYNAMIC-INFORMATION | operation_metadata | secret_read_dynamic | Information Disclosure | secret_read_dynamic may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECRET_READ_DYNAMIC-ELEVATION | operation_metadata | secret_read_dynamic | Elevation of Privilege | secret_read_dynamic may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECURITY_SCAN-TAMPERING | operation_metadata | security_scan | Tampering | security_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECURITY_SCAN-REPUDIATION | operation_metadata | security_scan | Repudiation | security_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECURITY_SCAN-INFORMATION | operation_metadata | security_scan | Information Disclosure | security_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |
| OP-SECURITY_SCAN-ELEVATION | operation_metadata | security_scan | Elevation of Privilege | security_scan may be abused or fail across browser_to_fastapi, fastapi_to_nats, nats_to_worker, worker_to_secrets_policy. | typed_operation_schema, openapi_validation, asyncapi_subject_governance, operation_audit_events, opa_policy_decision, secret_redaction, vault_or_openbao_leases | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. New secret or policy fields may require manual redaction review. |

## Mitigations

| Mitigation | Covers | Evidence |
|---|---|---|
| asyncapi_subject_governance | Tampering, Repudiation, Denial of Service, Elevation of Privilege, Information Disclosure | operations/*.yaml |
| correlation_ids | Tampering, Repudiation, Denial of Service, Elevation of Privilege | operations/*.yaml |
| dlq_review_path | Tampering, Repudiation, Denial of Service, Elevation of Privilege | operations/*.yaml |
| event_redaction | Tampering, Repudiation, Information Disclosure, Denial of Service | operations/*.yaml |
| event_sourced_workflow_journal | Tampering, Repudiation, Denial of Service, Elevation of Privilege | operations/*.yaml |
| opa_policy_decision | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | operations/*.yaml |
| openapi_validation | Tampering, Repudiation, Denial of Service, Elevation of Privilege, Information Disclosure | operations/*.yaml |
| operation_audit_events | Tampering, Repudiation, Denial of Service, Elevation of Privilege, Information Disclosure | operations/*.yaml |
| payload_minimization | Tampering, Repudiation, Information Disclosure, Denial of Service | operations/*.yaml |
| secret_redaction | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | operations/*.yaml |
| typed_operation_schema | Tampering, Repudiation, Denial of Service, Elevation of Privilege, Information Disclosure | operations/*.yaml |
| vault_or_openbao_leases | Tampering, Repudiation, Information Disclosure, Elevation of Privilege | operations/*.yaml |

## Residual Risks

| Operation | Risk | Owner | Treatment |
|---|---|---|---|
| backup_now | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| backup_now | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| backup_verify | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| backup_verify | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| catalog_refresh | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| configure_opa | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| configure_opa | New secret or policy fields may require manual redaction review. | feature_owner | Review during operation metadata and Threat Dragon review. |
| deploy_blueprint | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_apply | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_apply | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_approve | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_approve | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_ignore | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_ignore | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_preview | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_preview | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_scan | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| drift_scan | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| fleet_join | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| fleet_join | New secret or policy fields may require manual redaction review. | feature_owner | Review during operation metadata and Threat Dragon review. |
| git_sync | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| health_check | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| health_check | Telemetry schemas can drift if new fields are added without privacy review. | feature_owner | Review during operation metadata and Threat Dragon review. |
| release_apply | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| release_apply | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| release_check | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| release_check | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| restore_backup | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| restore_backup | Edge-first deployments remain sensitive to host, storage, and network availability. | feature_owner | Review during operation metadata and Threat Dragon review. |
| rotate_secret | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| rotate_secret | New secret or policy fields may require manual redaction review. | feature_owner | Review during operation metadata and Threat Dragon review. |
| secret_read_dynamic | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| secret_read_dynamic | New secret or policy fields may require manual redaction review. | feature_owner | Review during operation metadata and Threat Dragon review. |
| security_scan | Authorized operators can still trigger high-impact changes if policy or approval controls are incomplete. | feature_owner | Review during operation metadata and Threat Dragon review. |
| security_scan | New secret or policy fields may require manual redaction review. | feature_owner | Review during operation metadata and Threat Dragon review. |

## OWASP Threat Dragon Local Review

Threat Dragon can be started locally for visual review/editing. Keep generated repository metadata authoritative and reconcile manual review notes back into version-controlled files.

```bash
task threatdragon:pull
task threatdragon:serve
# open http://localhost:8082
```

## Validation

```bash
task docs:threat-model:check
task docs:threat-model
mkdocs build --strict
```
