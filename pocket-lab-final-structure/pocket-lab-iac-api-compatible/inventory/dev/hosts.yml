---
all:
  children:
    common:
      hosts:
        localhost:
          ansible_connection: local
    vault:
      hosts:
        localhost:
          ansible_connection: local
    mariadb:
      hosts:
        localhost:
          ansible_connection: local
    gitea:
      hosts:
        localhost:
          ansible_connection: local
    opa:
      hosts:
        localhost:
          ansible_connection: local
    tailscale:
      hosts:
        localhost:
          ansible_connection: local
    caddy:
      hosts:
        localhost:
          ansible_connection: local
    dashboard_api:
      hosts:
        localhost:
          ansible_connection: local
    catalog_seed:
      hosts:
        localhost:
          ansible_connection: local
    workloads:
      hosts:
        localhost:
          ansible_connection: local
    backups:
      hosts:
        localhost:
          ansible_connection: local
    gitea_runner:
      hosts:
        localhost:
          ansible_connection: local
    drift_check:
      hosts:
        localhost:
          ansible_connection: local
    observability:
      hosts:
        localhost:
          ansible_connection: local